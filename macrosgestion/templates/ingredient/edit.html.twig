{% extends 'base.html.twig' %}

{% block title %}Edit Ingredient{% endblock %}

{% block body %}
<div class="container mx-auto my-12 p-6 max-w-3xl bg-white shadow-xl rounded-lg">
    <h1 class="text-3xl font-extrabold text-black mb-6">Edit Ingredient</h1> <!-- Changed text-gray-800 to text-black -->

    <div class="mb-6">
        {{ include('ingredient/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="flex justify-between items-center mt-6">
        <a href="{{ path('app_ingredient_index') }}" 
           class="inline-block bg-gray-500 text-white font-medium text-lg py-2 px-4 rounded-lg shadow hover:bg-gray-600 transition duration-300">
            Back to List
        </a>
    </div>
</div>
{% endblock %}
